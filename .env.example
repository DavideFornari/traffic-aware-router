# Copy this file to `.env` (which is gitignored — never commit the real one)
# and fill in a real key to enable live traffic without pasting it into the
# sidebar every session. Get a free key at developer.tomtom.com — no credit
# card required (see README.md's "Configuration" section).

TOMTOM_API_KEY=
